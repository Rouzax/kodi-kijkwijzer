# Manual rating overrides for movies
# Use this file to set ratings for movies that no API can resolve.
# The title must match exactly as it appears in your Kodi library.
#
# Copy this file to overrides_movies.yaml and add your overrides.
# For TV shows, create overrides_tvshows.yaml with the same format.

overrides:
  # "Sprookjesboom de Musical": "AL"
  # "Woezel En Pip - Alles Is Fijn Familiemusical": "AL"
